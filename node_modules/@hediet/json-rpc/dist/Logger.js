/**
 * A `RpcLogger` that prints everything to `console`.
 */
export class ConsoleRpcLogger {
    debug(logEntry) {
        console.log(logEntry.text, logEntry.exception);
    }
    warn(logEntry) {
        console.warn(logEntry.text, logEntry.exception);
    }
    trace(logEntry) {
        console.log(logEntry.text, logEntry.exception);
    }
}
//# sourceMappingURL=Logger.js.map